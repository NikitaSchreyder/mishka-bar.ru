export type TMenuCategory = {
    name: string
    searchLink: string
    thumbUrl: string
}

export type TMenuItem = {
    name: string
    description: string
    price: number
    thumbUrl: string
    categorySearchLink: string
}